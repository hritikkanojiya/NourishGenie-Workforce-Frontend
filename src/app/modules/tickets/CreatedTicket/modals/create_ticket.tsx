export interface user_id {
    loginUserId: any
}

export interface prev_state {
    appAgents: number,
    agentsCount: number
}

export interface pagination_prev_state {
    itemsPerPage: number,
    showingFrom: number,
    showingTill: number,
    page: number,
}